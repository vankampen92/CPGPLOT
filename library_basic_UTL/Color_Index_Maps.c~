#include "cpgplot_headers.h"

void color_Index_into_RGB_Map(int color_Index, int icilo, int icihi,  
			      float * R, float * G, float * B)
{ 
  /* This function tranlates a color index between icilo and 
     icihi into a RGB defined color. Simply, these color components 
     are the output of this function. 
  */
  
  * G = 1.0; * B = 0.0; 
   
  * R = (float)(color_Index-icilo)/ (float)(icihi-icilo); 
}
